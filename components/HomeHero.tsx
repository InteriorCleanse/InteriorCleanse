'use client'
import {useEffect,useRef,useState} from 'react'
import {SplineScene} from './SplineScene'
import {SPLINE_SCENES} from '@/lib/spline-scenes'
export function HomeHero(){const title=useRef<HTMLHeadingElement>(null);useEffect(()=>{const f=()=>{if(title.current)title.current.style.transform=`translateY(${scrollY*.3}px)`};addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]);return <section className="home-hero"><img src="https://images.unsplash.com/photo-1616486338812-3dadae4b4ace?w=1800&q=90" alt="" aria-hidden="true" style={{position:'absolute',inset:0,width:'100%',height:'100%',objectFit:'cover',objectPosition:'center',opacity:.35,zIndex:0,filter:'saturate(0.6) contrast(1.1)'}}/><div className="hero-orbit"/><SplineScene url={SPLINE_SCENES.hero} className="spline-hero-bg" lazyLoad={false}/><div className="grain"/><p className="hero-edition">VOLUME 01<br/>EST. 2026</p><h1 ref={title}>The art of<br/>the considered<br/><em>home.</em></h1><div className="hero-scroll"><span>Scroll</span><i/></div></section>}
export function EmailCapture(){const [value,setValue]=useState('');const [done,setDone]=useState(false);return <div className="email-line"><input aria-label="Email address" value={value} onChange={e=>setValue(e.target.value)} placeholder="YOUR EMAIL ADDRESS" type="email"/><button onClick={()=>value.includes('@')&&setDone(true)}>{done?'WELCOME TO THE EDIT':'SUBSCRIBE →'}</button></div>}
