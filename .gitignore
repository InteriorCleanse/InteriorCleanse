.DS_Store
.env*.local
.next
node_modules
out/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Brand master originals are preserved but not needed by the build
raw-clips/

# Packaged copy of trading-bot/ for download; duplicates tracked source
paper-trading-bot.zip
