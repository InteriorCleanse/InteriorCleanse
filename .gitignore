node_modules
.next
out
build
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Secrets — never commit. .env.example is the only env file that ships.
.env
.env.local
.env*.local

# Supabase CLI local state
supabase/.branches
supabase/.temp
