import {allProducts} from '@/lib/content'; import {ProductCard} from '@/components/cards';
export default function Shop(){return <><section className="shop-hero"><p className="eyebrow">Commerce / volume 01</p><h1>The considered<br/><em>object index.</em></h1><p>Objects selected not for novelty, but for how beautifully they serve.</p></section><section className="shop-index"><div className="shop-filter"><span>FILTER</span><span>ALL OBJECTS / {String(allProducts.length).padStart(2,'0')}</span><span>2026 EDITION</span></div><div className="shop-grid">{allProducts.map((p,i)=><ProductCard product={p} index={i} key={p.slug}/>)}</div></section></>}
